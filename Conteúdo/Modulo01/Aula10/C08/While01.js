//-------------------------------------- Exemplo 01 ------------------------------------------------------------------*/

const cores = ["Branco", "Azul", "Amarelo", "Verde", "Roxo", "Roso"];
let index = 0;

// // length tamanho da lista:
while (index < cores.length) {
  console.log(index, cores[index]);
  index++; // //index = index + 1 ou index += 1
}
